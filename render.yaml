services:
  - type: web
    name: dubi-frontend
    runtime: static
    buildCommand: pnpm run build:web
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
